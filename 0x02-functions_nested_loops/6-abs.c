#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: number to be computed
 * Return: returns absolulte value of integer
**/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}

}
